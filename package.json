{
  "name": "vesta-workspaces-app",
  "version": "1.0.0",
  "description": "Backend functions for Vesta Workspaces",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --define:process.env.API_KEY='\"'$API_KEY'\"' && cp index.html metadata.json dist/"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.8"
  },
  "devDependencies": {
    "@netlify/functions": "^2.6.0",
    "typescript": "^5.4.5",
    "esbuild": "^0.20.2"
  }
}