
{
  "name": "vesta-workspaces-app",
  "version": "1.0.0",
  "description": "An AI-powered collaborative platform for analyzing project plans.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --define:process.env.API_KEY='\"'$API_KEY'\"' --external:@google/genai --external:jspdf && cp index.html metadata.json dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "esbuild": "^0.20.2"
  }
}